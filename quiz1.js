const a = maxvalue([1, 2, 3, 4, 5]);
console.log(a);

let maxValue;
let sortArr;

for (let i = a.length - 1; (i) => 0; i--) {
  if (a[i] > max) {
    max = a[i];
  }
  sortArr.add = a[i];
}

maxValue = max;

console.log(maxValue, sortArr);
